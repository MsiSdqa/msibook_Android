<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false">

    <!-- 实心 -->
    <solid android:color="#d21e25" />
    <!-- 圆角 -->
    <corners android:radius="360dp" />
    <!-- 边距 -->
    <padding
        android:bottom="0dp"
        android:left="0dp"
        android:right="0dp"
        android:top="0dp" />

    <!-- 大小 -->
    <size android:width="15dp"
        android:height="15dp" />

</shape>
